CREATE TABLE addresses (

id INT AUTO_INCREMENT PRIMARY KEY,

user_id INT,

street VARCHAR(255),

city VARCHAR(100),

zipcode VARCHAR(20),

country VARCHAR(100),

FOREIGN KEY(user_id)

REFERENCES users(id)

);