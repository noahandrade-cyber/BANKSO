(()=>{const target=new Date('2026-09-15T18:00:00+02:00').getTime(),ids=['days','hours','minutes','seconds'];function tick(){let n=Math.max(0,target-Date.now());const parts=[Math.floor(n/864e5),Math.floor(n/36e5)%24,Math.floor(n/6e4)%60,Math.floor(n/1e3)%60];ids.forEach((id,i)=>document.getElementById(id).textContent=String(parts[i]).padStart(2,'0'))}tick();setInterval(tick,1000);const form=document.getElementById('waitlist-form'),notice=document.querySelector('.confirmation');form.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button');button.disabled=true;try{const r=await fetch('/api/waitlist',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.email.value})}),d=await r.json();if(!r.ok)throw new Error(d.error);notice.textContent='✓ Merci ! Votre accès anticipé est enregistré.';notice.style.color='#00c8f5';form.reset()}catch(err){notice.textContent=err.message||'Une erreur est survenue.';notice.style.color='#ff9e9e'}finally{button.disabled=false}})})();
